apiVersion: apps/v1
kind: Deployment
metadata:
  name: KMPCube
  labels:
    app: KMPCube
spec:
  replicas: 1
  selector:
    matchLabels:
      app: KMPCube
  template:
    metadata:
      labels:
        app: KMPCube
    spec:
      containers:
        - name: KMPCube
          image: valden01/cubekmp:tagname
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-pass
                  key: mysql-user-password
            - name: DB_SERVER
              valueFrom:
                configMapKeyRef:
                  name:  mysql-config-map
                  key: mysql-server
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name:  mysql-config-map
                  key: mysql-database-name
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: mysql-user-username
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: KMPCube
  labels:
    app: KMPCube
spec:
  type: LoadBalancer
  selector:
    app: KMPCube
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
      nodePort: 30000