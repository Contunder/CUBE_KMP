apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config-map
data:
  mysql-server: KMPCube    # The name of mysql service
  mysql-database-name: KMPCube     # The name of the database used by the demo app
  mysql-user-username: KMPCube     # A new created user for the demo app